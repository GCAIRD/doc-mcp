# MCP Server URL (must be running before tests)
MCP_BASE_URL=http://localhost:8902

# Voyage API Key (only needed if testing embedding quality)
# VOYAGE_API_KEY=

# For agent e2e tests (Layer 2, future)
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
