name: Deploy EN

on:
  push:
    branches: [feature/en-version]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: build tutorial and restart server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd MCS-DOC-MCP-en
            git pull
            cd tutorial
            npm run build
            cd ..
            docker compose down
            docker compose build
            docker compose up -d
